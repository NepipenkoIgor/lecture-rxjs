//
// const sequence = new Promise((res) => {
//     let count = 1;
//     setInterval(() => {
//         res(count++);
//     }, 1000)
// })
//
// sequence.then((v: any) => terminalLog(v))
// sequence.then((v: any) => terminalLog(v))
// sequence.then((v: any) => terminalLog(v))

// const sequence  = function* iteratorFn () {
//   let item = 1;
//   while (true){
//       yield item++;
//   }
// }()
//
// terminalLog(sequence.next().value);
// terminalLog(sequence.next().value);
// terminalLog(sequence.next().value);
// terminalLog(sequence.next().value);
// terminalLog(sequence.next().value);
