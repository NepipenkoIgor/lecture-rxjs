// import { fromEvent } from "rxjs";
// import { pluck } from "rxjs/operators";
//
//
// fromEvent(document,'click')
//  .pipe(
//      pluck('clientX')
//  ).subscribe(singleton.setData.bind(singleton))
